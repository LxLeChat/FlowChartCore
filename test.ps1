$a={
   foreach ($item in $collection) {
       if ($a) {
           "if"
       } elseif ($b) {
           "elseif"
       } else {
           "else"
       }

       for ($i = 0; $i -lt $array.Count; $i++) {
           "for"
       }

       while ($a) {
           "while"
       }

       do {
           "dowhile"
       } while ($c)

       do {
           "dountil"
       } until ($c)

       try {
           "try"
       }
       catch {
           "catch"
       } finally {
           "finally"
       }
}